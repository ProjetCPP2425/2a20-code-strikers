#ifndef MODIF_DATE_H
#define MODIF_DATE_H
#include <QDialog>

class SmallWindow : public QDialog {
    Q_OBJECT

public:
    explicit SmallWindow(QWidget *parent = nullptr);
};
#endif // MODIF_DATE_H
